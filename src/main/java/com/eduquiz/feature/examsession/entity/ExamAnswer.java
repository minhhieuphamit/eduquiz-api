package com.eduquiz.feature.examsession.entity;

/**
 * @Entity exam_answers
 * Fields: id, session(@ManyToOne), question(@ManyToOne),
 * selectedAnswer(CHAR nullable), isCorrect(Boolean nullable)
 * TODO: @Entity @Data
 */
public class ExamAnswer {
}
