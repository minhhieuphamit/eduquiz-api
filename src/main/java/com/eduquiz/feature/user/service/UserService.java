package com.eduquiz.feature.user.service;

/**
 * User Management Service (Admin).
 * - getAllUsers(Pageable) → Page<UserResponse>
 * - getUserById(id) → UserResponse
 * - updateUserRole(id, role) → UserResponse
 * - deactivateUser(id) → void
 * TODO: Implement @Service
 */
public class UserService {
}
