package com.eduquiz.feature.examroom.repository;

/**
 * JpaRepository<RoomParticipant, Long>
 * - findByRoomIdAndUserId(roomId, userId) → Optional
 * - findByRoomId(roomId) → List
 * - countByRoomId(roomId) → int
 * TODO: Implement
 */
public interface RoomParticipantRepository {
}
