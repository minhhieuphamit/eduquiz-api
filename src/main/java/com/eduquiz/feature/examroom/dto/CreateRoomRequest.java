package com.eduquiz.feature.examroom.dto;

/**
 * Fields: title, examId, randomMode, poolSize(cho POOL mode),
 * startTime, endTime, maxStudents(optional)
 * TODO: @Data + validation
 */
public class CreateRoomRequest {
}
