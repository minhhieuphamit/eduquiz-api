package com.eduquiz.feature.exam.dto;

/**
 * Fields: title, subjectId, durationMinutes(optional, default theo môn),
 * totalQuestions, randomMode, difficulty(optional filter),
 * questionIds(cho MANUAL mode)
 * TODO: Implement @Data
 */
public class CreateExamRequest {
}
