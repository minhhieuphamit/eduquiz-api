# TODO: Sử dụng ở Phase 7 (ELK)
input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  # Parse timestamp, add metadata
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "eduquiz-logs-%{+YYYY.MM.dd}"
  }
}
